# action.yml

on:
  push:
    paths:
      - .github/workflows/test.yaml

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 'Hello World'
        description: 'Custom greet with golang program'
        inputs:
        outputs:
        run:
          using: 'docker'
          image: 'Dockerfile'
          args:
